apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: xae
  namespace: argocd

spec:
  description: XAE Platform

  sourceRepos:
    - '*'

  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc

  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
